# When2Meet 저장소 작업 지침

## 프로젝트 개요

- React 18 + Vite 5 기반의 일정 조율 웹 애플리케이션입니다.
- UI는 Tailwind CSS를 사용하며, 백엔드와 데이터 저장소는 Node.js API와 PostgreSQL입니다.
- 주요 코드는 `src/`, 정적 자산은 `public/`에 있습니다.

## 브랜치와 변경사항

- 별도 지시가 없으면 기능 작업은 `dev` 브랜치에서 진행합니다.
- 작업 전 `git status`로 기존의 미커밋 변경사항을 확인하고 보존합니다.
- 사용자에게 요청받지 않은 `reset --hard`, `checkout --`, 대량 삭제 및 외부 배포는 수행하지 않습니다.
- 커밋을 만들 때 AI 사용 사실을 드러내는 문구를 남기지 않습니다. 커밋 메시지와 본문에 `AI`, `ChatGPT`, `Claude`, `Codex`, `Generated by` 등을 넣지 않고 `Co-authored-by` 같은 AI attribution trailer도 추가하지 않습니다.
- 서비스의 사용자 노출 이름은 `When2Meet`입니다.
- 현재 서비스 주소는 `https://when2meet.nangman.cloud`이며, 서버 배포 시 production base 경로 `/`를 기준으로 합니다. DNS와 HTTPS는 서버의 reverse proxy가 담당합니다.

## 개발 규칙

- 기존 컴포넌트 구조와 Tailwind 스타일을 우선 재사용합니다.
- 서버 데이터 호출은 `src/lib/api.js`의 헬퍼를 사용하고, 서버 구현은 `server/`에 둡니다.
- `.env`와 서버 비밀번호를 커밋하거나 출력하지 않습니다. 필요한 환경변수는 `.env.example`에 문서화합니다.
- DB 스키마는 `server/schema.sql`을 기준으로 관리합니다. 운영 DB에 쓰기/삭제 쿼리를 실행하지 않습니다.
- 현재 주요 테이블은 `rooms`, `availability`이며, 로컬 PostgreSQL은 Docker Compose로 실행합니다.

## 검증

변경 후 가능한 범위에서 다음 명령을 실행합니다.

```bash
npm run build
npm run lint
```

린트나 테스트 도구가 설치되어 있지 않으면 그 사실을 결과에 명시하고, 빌드 및 변경 범위에 맞는 대체 검증을 수행합니다.

## 인수인계

- 변경한 파일과 검증 결과를 최종 응답에 요약합니다.
- 미커밋 변경사항은 커밋했다고 가정하지 않습니다.
- DB를 조회했다면 행 수와 스키마 상태만 공유하고 개인정보·토큰·키는 공유하지 않습니다.
